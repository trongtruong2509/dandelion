import React from "react";
import { initMoodeAndActivity } from "../../common/utils/genres";

const Radio = () => {
   return (
      <div className="w-full">
         <div
            className="w-full h-[600px] flex-center text-primary"
            // onClick={() => initMoodeAndActivity()}
         >
            NOT IMPLEMENTED YET
         </div>
      </div>
   );
};

export default Radio;
